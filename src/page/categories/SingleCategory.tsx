import {FC} from 'react';

const SingleCategory:FC = () => {
    return (
        <main className={'container'}>
            Single Categories
        </main>
    );
};

export default SingleCategory;